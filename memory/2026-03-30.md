
## 02:13-02:40 自驱工作（gateway-stability-fix 分支）
1. gateway诊断：systemd Restart=always已配置，01:50-02:07死亡是手动restart前未跑，非自动
2. snapshot.js v2：解析frontmatter替代原始dump，tasks.json干净
3. USER.md：建立用户画像记录已知偏好
本分支新commits: c4ca90e(feat/diag.sh), 48149fd(docs/总文档补充v1.2.5)
教训：bash heredoc中单引号才能防展开；heartbeat自动触发commit不计入本人贡献

## 02:42-03:00 自驱工作

### internal_task_pool.py 彻底修复
- 根因：5个exec并发启动时 flock get_lock() 无效，save_pool 写损坏JSON
- 修复：get_lock() → acquire_lock()（flock）先于所有逻辑
- 修复：移除 git stash / git commit -a 调用（运行产物混入）
- 修复：save_pool 写入换行符
- commit: be3c218 fix: internal_task_pool flock防止并发重入 + 移除git stash/commit

### 教训
- flock 必须先于所有逻辑立即执行，不能先检查后加锁
- write工具会把\n转义为\n，用Python heredoc写入
- heartbeat batch调用for循环有并发风险，需要真实文件锁


## 03:00-03:10 自驱工作

### write工具破坏\n教训（重要）
- write工具把Python字符串中的\n转义成\\n（两个字符），导致f.write("\\n")变成字面"\\n"
- 解决：用python3 heredoc或python3 -c写入文件，避免用write工具写代码文件
- 精准修复：sed -i '54s/.*/        f.write("\\n")/' + python3 -m py_compile验证

### .gitignore彻底解决方案
- 运行产物(portal/status/*.json, internal_pool.json, reports/*.log)必须不追踪
- .gitignore写规则后，还需git rm --cached <file>移除已追踪记录
- git check-ignore -v <file>验证是否生效

### commit记录
- beded08 fix: save_pool换行符修复
- ff693c6 chore: .gitignore运行产物移出追踪
- fa63c00 chore: portal/index.md简化

## 03:00-03:14 自驱工作

### 完成项目
1. internal_task_pool.py flock修复 + 移除git stash/commit
2. diag.sh 轻量自检脚本
3. snapshot.js v2（frontmatter解析）
4. USER.md 用户画像建立
5. 实施总文档 v1.2.5 更新
6. TOOLS.md 路径表更新

### gateway-stability-fix 分支 commits
- 4c89e9d fix: internal_task_pool flock防止并发重入
- c4ca90e feat: scripts/diag.sh
- 48149fd docs: 实施总文档v1.2.5

### 重要教训
- flock必须立即执行，不能先检查后加锁（并发5路同时启动时检查在锁创建前完成）
- write工具把\n转义为\\n，必须用Python heredoc写入
- for循环5个exec并发启动时，每个exec是独立进程，lock file检查无原子性

### Token状态（03:14）
- 当前：~4055/4500（90.1%）
- 足够，支持继续自驱

